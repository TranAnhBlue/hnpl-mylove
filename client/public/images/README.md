# Hướng dẫn thêm ảnh background

Đặt ảnh background của bạn vào thư mục này với tên `background.jpg`

Hoặc nếu dùng tên khác (ví dụ: `my-photo.png`), cập nhật trong file `client/src/App.css`:

```css
background: url('/images/my-photo.png');
```

Định dạng ảnh hỗ trợ: .jpg, .jpeg, .png, .webp
